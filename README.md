# LHP

LHP (Local Hosting Provider) is a simple webpanel that intends to simulate a hosting provider for use in learning purposes.

## Requirements

Docker

pfSense (any DNS server with an API would work, but LHP only supports pfSense currently)
